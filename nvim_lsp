Если возникнут проблемы с конкретным LSP (например, clangd не видит заголовочные файлы), 
нужно будет создать compile_commands.json через CMake. Это делается так:

mkdir build && cd build
cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON ..
ln -s build/compile_commands.json ../
